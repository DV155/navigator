# navigator
Python-based script which calls onto geoapify to craft a route between two places and visuales it in an html page

# steps

Executed in two main parts: a python-tool which uses geoapify to calculate distance between two spots (and mean walking time between them) with the haversine formula, as well as visualisation of the data achieved in part 1 using html and js
